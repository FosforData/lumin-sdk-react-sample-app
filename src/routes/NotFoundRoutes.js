// project import
import NotFound from 'pages/notFound/index';

// ==============================|| AUTH ROUTING ||============================== //

const NotFoundRoutes = {
    path: '*',
    element: <NotFound />
};

export default NotFoundRoutes;
